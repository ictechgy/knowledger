# Design Contract

## Source of truth

- [AGENTS.md](AGENTS.md), [프로토콜 설계](docs/07-DELIVERY-PLAN.md): 승인·기밀·VALID commit의 필수 경계.
- [index.html](apps/web/index.html), [style.css](apps/web/style.css), [app.js](apps/web/app.js): 현재 구현 근거. 기본 HTML/CSS/JavaScript 앱이다.
- [실행 범위](docs/11-RUNTIME.md): 실제 제공하는 기능과 운영 미검증 범위.
- [Claude 디자인 논의](docs/18-DESIGN-REVIEW.md): 사용자가 요청한 검토. `d9bd6eb` 시점 화면 코드3개를 Claude Sonnet5로 검토했다. 제안은 아래 구현 제약보다 우선하지 않는다.
- 검토함·작업 공간 내비게이션·기술 증거 접기·모바일 세로 목록을 구현했다. 검증 범위와 후속 작업은 아래에 구분한다.

## Brand

KCL은 조직이 지식의 의미를 검토하고 합의하는 도구다. 조직·맥락·승인 역할은 프로젝트 설정에서 정한다. 현재 이름·로고·어두운 팔레트를 유지한다.
로그인 후에는 제품 소개보다 사용자의 문서와 검토 작업을 앞세운다. 새로운 브랜드·이미지·마케팅 문구를 만들지 않는다.

## Product goals

- 현재 합의 상태, 내가 해야 할 검토, 비공개 초안의 위치를 바로 찾는다.
- 비공개 저장·공유 게시·합의 활성·실행 컨텍스트 제공을 서로 다른 단계로 이해한다.
- 상태를 확인할 수 없으면 보류 이유와 다음 행동을 보여준다.
- 기술적인 증거를 필요한 사람이 확인할 수 있게 하면서 기본 화면의 해독 부담을 줄인다.

## Personas and jobs

- 작성자: 비공개 초안 작성/가져오기, 검토 재개, 공개 범위 확인.
- 지정 대표자: 정확한 개정 원문 확인, 근거와 승인/이의/기권 제출, 합의 진행 확인.
- 지식 사용자: 업무 범위에 맞는 공유 문서를 찾고 새 컨텍스트 검증 후 사용.
- 운영자: checkpoint·transaction·policy·manifest와 장애 근거 확인.
- AI actor는 초안을 만들 수 있으나 사람 승인 버튼의 권한을 얻지 않는다.

## Information architecture

채택한 방향은 **검토함 / 문서·초안 / 실행 컨텍스트**의 세 작업 공간이다. 현재는 한 화면의 세 영역을 내비게이션으로 연결하며, 별도 페이지를 만들지는 않는다.

- 검토함: 내 미응답 제안을 표시한다. 이의가 있는 합의는 해당 문서의 합의 검토에서 확인한다. 단순 승인 수로 실행 권한을 판단하지 않는다.
- 문서·초안: 공유 문서와 내 비공개 초안을 별도 목록으로 유지한다. 선택한 개정 상세에서 검토/편집으로 이동한다.
- 실행 컨텍스트: 범위 선택·fresh fence 결과·재검증을 함께 둔다.
- 대표자에게 처리할 검토가 있으면 검토함을 우선 제안한다. 작성자와 조회 사용자에게 빈 검토함을 강제하지 않는다.
- raw ID·digest·전체 manifest는 증거 상세의 접기/펼치기로 제공한다. 작업에 필요한 문서 제목·범위·수신 조직은 바로 보인다.

## Design principles

- 한 화면의 주 행동을 명확히 한다. 비공개 저장과 공용 게시를 같은 강조 수준으로 나열하지 않는다.
- 상태마다 텍스트를 붙이고 색상만으로 구별하지 않는다.
- 같은 의미의 상태 이름을 목록·상세·안내에서 통일한다.
- 공유 확인은 항상 실제 snapshot 원문과 수신 조직을 보여준 뒤 받는다.
- 오류·pending·withheld가 성공 알림처럼 자동으로 사라져 확정된 것으로 오해되지 않게 한다.

## Visual language

현재 토큰: canvas `#0d1117`, surface `#151b25`, ink `#f4f1eb`, muted `#9ca5b4`,
teal `#65dbc4`, yellow `#f5bf69`, red `#f27782`, blue `#84b8ff`.
활성 합의는 teal, 진행 중 검토는 yellow, 중지/철회/제공 보류는 red, 게시 후 합의 전은 중립 또는 blue로 구별한다.
환경 배지와 상태 배지의 시각적 우선순위는 후속 세부 조정 대상이다.

본문은 현재 sans-serif, 기술 증거는 monospace를 유지한다. 작업 화면의 큰 소개 문구를 작은 sans-serif “공유 지식 검토” 제목으로 바꿨다. 읽어야 하는 안내·metadata는 14px 이상을 후속 목표로 삼는다.
현재 faint `#687385`는 surface 대비3.60:1이므로 작은 필수 텍스트에는 사용하지 않는다.
muted는 같은 배경 대비6.96:1이다. 이 값은 토큰 계산이며 렌더링 전체의 접근성 인증이 아니다.

기존 panel 여백 약1.25–1.35rem, 입력/버튼 radius7px, panel 간격을 유지한다. 과도한 새 shadow·gradient·장식은 추가하지 않는다.
기능을 설명할 필요가 없는 이미지는 사용하지 않는다. 짧은 hover/focus 전환을 유지하고 reduced-motion을 따른다.

## Components

- 상태 배지: 초안 / 공유 게시됨·합의 전 / 합의 검토 중 / 합의 활성 / 정지 / 철회 / 대체 / 현재 사용 보류.
- 비공개 초안: 본인 목록·개수, 새로고침·더 보기, 원본을 보존한 수정본 생성.
- 공개 검토: 원문 snapshot·수신 조직·확인 checkbox·게시 버튼. 내용을 바꾸면 이전 preview를 폐기한다.
- 대표자 검토: 담당 역할·현재 결정·근거 입력. 프런트엔드 집계가 서버의 승인 판정을 대체하지 않는다.
- 컨텍스트 결과: provided/withheld·이유·반환 문서·접을 수 있는 manifest·재검증 행동.
- 기술 증거: 키보드 접근 가능한 details/summary. 숨김은 열람 편의이며 접근 제어가 아니다.
- 미확정 거래 안내: 시간 경과만으로 성공/실패로 바꾸지 않는다. 지속적인 거래별 추적 UI는 후속 구현 범위다.

## Accessibility

기본 button/label/input/details 요소를 우선한다. 제목 계층·연결된 label·보이는 focus·Tab 순서를 유지한다.
작은 일반 텍스트는 대비4.5:1 이상을 목표로 검증하고, 상태색에는 항상 이름을 붙인다.
중요 상태 변화는 live region으로 알린다. 장애/미확정 안내를 읽을 시간을 임의로 제한하지 않는다.
큰 본문·JSON에는 목적과 요약을 먼저 제공하고, 원문 전체를 명시적으로 열람할 수 있게 한다.

## Responsive behavior

현재 breakpoint는1050/760/460px이다. 760px 이하 문서 카드를 세로 목록으로 정리했다.
휴대전화에서는 목록→선택한 문서→해당 행동 순서로 이동하며 증거는 접는다.
390px와1440px에서 가로 넘침·focus 이동·본문 열람을 검증한다. 큰 표/코드는 영역 안에서만 스크롤한다.

## Interaction states

- Loading: 어느 목록/문서를 확인 중인지 표시하며 중복 제출을 제한한다.
- Empty: 없음과 권한/네트워크 오류를 구분한다. 다른 actor의 문서 수·제목을 힌트로 주지 않는다.
- Error: 안전한 오류와 재시도 행동. 비밀정보·내부 경로·토큰을 표시하지 않는다.
- Disabled: 필요한 근거·권한·공개 확인의 이유를 알린다.
- Pending: 아직 VALID가 아님을 유지한다. ACK·이벤트만으로 성공으로 바꾸지 않는다.
- Provided/withheld: fresh fence의 해당 시점 결과다. 이후 사용에는 재검증이 필요하다.
- 계정 변경/로그아웃: private 목록·원문·preview를 지우고 늦은 이전 응답을 표시하지 않는다.
- 저장된 초안 편집: 원본과 범위를 보존하고 새 초안으로 저장한다.

## Content voice

짧고 구체적인 한국어를 쓴다. 기본 행동은 “초안 저장”, “공유 검토”, “공용 원장에 게시”, “승인”,
“이의 제기”, “합의 활성화”, “컨텍스트 확인”처럼 결과를 설명한다.
게시를 합의 완료로, browse의 활성 표시를 즉시 실행 허가로 표현하지 않는다. 개발 계정/로컬 실행 표시는 유지한다.

## Implementation constraints

백엔드·인증·조직 범위·서명·복구 코드가 우선이다. 새로운 UI 프레임워크나 의존성은 이번 디자인 논의의 요구가 아니다.
공통 도메인 규칙, actor 인가, CSRF, immutable revision, approval binding, VALID commit 검증을 보존한다.
브라우저가 조직/승인 역할을 임의로 바꾸지 않는다. 같은 OS/loopback의 조직 실행은 독립 기관의 보안 경계로 표현하지 않는다.
레이아웃 변경 전에 현재 작업의 기준 화면을 확보하고, 구현 뒤 브라우저·접근성·기능 흐름을 검증한다.

## Open questions

- 실제 파일럿에서 가장 자주 들어오는 역할과 우선 작업: 제품 담당자가 사용 패턴으로 확인한다. 현재는 역할별 진입을 유지한다.
- 조직/업무의 범위 이름: 프로젝트 JSON으로 입력한다. 특정 산업의 이름은 예제에만 둔다.
- 별도 화면 전환과 상세 타이포 조정은 실제 사용 피드백에 따라 결정한다. 현재 내비게이션과 390/600/1440px DOM 레이아웃 검증은 완료했다.
